# re模块，
import re

# re.match():根据正则的规则，从头开始匹配数据，一旦跟咱们定制的正则匹配规则不符，就会返回None
# re.search():根据正则的规则，在全文中检索，一旦有符合正则规则的，就会立即返回结果，如果全文都没匹配到，那也会给我们返回None
# re.findall():根据正则的匹配规则，在全文中检索，返回所有符合规则的数据结果
# re.sub():写一个正则的匹配规则，将你需要替换的字符串写入，就可以时间替换
# re.split()：根据你设置的分割符号，来分割字符串